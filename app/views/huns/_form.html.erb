<%= form_for(@hun) do |f| %>
  <% if @hun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hun.errors.count, "error") %> prohibited this hun from being saved:</h2>

      <ul>
      <% @hun.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
